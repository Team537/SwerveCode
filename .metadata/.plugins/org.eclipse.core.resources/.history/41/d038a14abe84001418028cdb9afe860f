#include "WPILib.h"
#include "Schematic.h"
#include "Thing.h"

class RobotDemo : public SimpleRobot
{
	Joystick Controller;
	CompressorManager compress;
	Thing SpdSet;
private:

public:
	RobotDemo(void):
		Controller(CONTROLLER)
	{
	
	} 
	void Autonomous()
	{
		
	}

	void OperatorControl()
	{
		while (IsOperatorControl())
		{
			comp.Start();
			SpdSet.Setspeed(Controller.GetRawAxis(2), Controller.GetRawButton(1));
		}
	}
	
	void Test() {

	}
};

START_ROBOT_CLASS(RobotDemo);
